---
applications:
- name: pasiphae
  memory: 1G
  instances: 1
  command: bundle exec rake cf:on_first_instance db:migrate && bundle exec rails serve -p $PORT -e production
  buildpack: ruby_buildpack
  services:
  - Pasiphae_DB
  - Pasiphae_Redis
